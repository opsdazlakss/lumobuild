rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- ESKİ YARDIMCI FONKSİYONLAR ---
    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }
    
    function isAdmin() {
      return request.auth != null && getUserData().role == 'admin';
    }
    
    function isModerator() {
      return request.auth != null && (getUserData().role == 'moderator' || isAdmin());
    }
    
    function onlyUpdatingReactions() {
      return request.resource.data.diff(resource.data).affectedKeys().hasOnly(['reactions']);
    }
    
    function onlyUpdatingUnreadMentions() {
      return request.resource.data.diff(resource.data).affectedKeys().hasOnly(['unreadMentions']);
    }
    function onlyUpdatingPollVotes() {
      return resource.data.type == 'poll' 
        && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['poll']);
    }
    // Users
    match /users/{userId} {
      allow read: if true;
      allow create: if request.auth != null && request.auth.uid == userId;
      allow update: if request.auth != null && (request.auth.uid == userId || isAdmin() || onlyUpdatingUnreadMentions());
      allow delete: if isAdmin();

      // Incoming Calls (EKLENDİ)
      match /incomingCalls/{callId} {
        allow read, write: if request.auth != null;
      }
    }

    // Servers
    match /servers/{serverId} {
      allow read: if request.auth != null;
      allow create: if isAdmin();
      allow update: if isAdmin();
      allow delete: if isAdmin();
      
      // Server Members
      match /members/{userId} {
        allow read: if request.auth != null;
        allow create: if request.auth != null;
        allow update: if isAdmin();
        allow delete: if isAdmin() || request.auth.uid == userId;
      }
      
      // Server Channels
      match /channels/{channelId} {
        allow read: if request.auth != null;
        allow write: if isAdmin();
        
        // Channel Messages
        match /messages/{messageId} {
          allow read: if request.auth != null;
          
          // --- ÇALIŞAN KISIM BURASI ---
          // getUserData().isMuted kontrolü bazen hata verdiği için
          // burayı "çalışan versiyondaki" gibi basit tuttum.
          allow create: if request.auth != null;
          // ----------------------------

          allow update: if request.auth != null && (
            request.auth.uid == resource.data.userId 
            || isModerator() 
            || onlyUpdatingReactions()
            || onlyUpdatingPollVotes()
          );
          allow delete: if request.auth != null && (request.auth.uid == resource.data.userId || isAdmin());
        }
        
        match /typing/{userId} {
          allow read, write: if request.auth != null;
        }
      }

      // Server Roles
      match /roles/{roleId} {
        allow read: if request.auth != null;
        allow write: if isAdmin();
      }
    }
    
    // Invite Codes
    match /inviteCodes/{code} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null;
      allow delete: if isAdmin();
    }
    
    // Roles
    match /roles/{roleId} { allow read: if request.auth != null; allow write: if isAdmin(); }
    
    // Admin Logs
    match /adminLogs/{logId} { allow read: if isAdmin(); allow create: if request.auth != null; allow update, delete: if isAdmin(); }
    match /dms/{dmId} {
      allow create: if request.auth != null;
      allow read: if request.auth != null && request.auth.uid in resource.data.participants;
      allow update: if request.auth != null && request.auth.uid in resource.data.participants;
      
      match /messages/{messageId} {
        allow read: if request.auth != null && 
                      request.auth.uid in get(/databases/$(database)/documents/dms/$(dmId)).data.participants;
        allow create: if request.auth != null && 
                      request.auth.uid in get(/databases/$(database)/documents/dms/$(dmId)).data.participants;
        allow update: if request.auth != null && request.auth.uid == resource.data.userId;
        allow delete: if request.auth != null && request.auth.uid == resource.data.userId;
      }
    }
    // Legacy support
    match /channels/{channelId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
      match /messages/{messageId} {
        allow read: if request.auth != null;
        allow create: if request.auth != null;
        allow update: if request.auth != null && (request.auth.uid == resource.data.userId || isModerator());
        allow delete: if request.auth != null && (request.auth.uid == resource.data.userId || isAdmin());
      }
      match /typing/{userId} {
        allow read, write: if request.auth != null;
      }
    }
    
    // System (Version Check)
    match /system/{docId} {
      allow read: if true; // Allow everyone (including unauthenticated on login screen if needed) to check version
      allow write: if isAdmin();
    }
  }
}